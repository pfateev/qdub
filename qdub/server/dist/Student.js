class Student {
    constructor(id, firstName, lastName, isTA) {
        this.id = id;
        this.firstName = firstName;
        this.lastName = lastName;
        this.isTA = isTA;
    }
}
export {};
//# sourceMappingURL=Student.js.map